
candidatos <- c("Lula", "Ciro", "Bolsonaro", "Doria", "Amoedo")

partidos <- c("PT", "PDT", "S/P", "PSDB", "NOVO")

ideologia <- c("esquerda", "esquerda", "extrema direita", "centro direita", "centro direita")

banco1 <- data.frame(candidatos, partidos, ideologia)